# Galactic Sovereign

## Run from a GitHub ZIP

After downloading and extracting the repository, open `src/index.html` directly
in a browser. The page automatically uses the checked-in standalone bundle for
`file://` launches, so the title-screen buttons remain interactive.

For development, install dependencies and use Vite instead:

```bash
npm install
npm run dev
```
